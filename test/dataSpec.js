define(['indexdb'], function(db) {
  'use strict';
  console.log(db);
  window.db = db;


  /*
   * It shall initialize the database
   * It shall open the database
   * It shall add a single tab
   * It shall add multiple tabs
   * It shall not add duplicate tabs
   * It shall remove a tab
   * It shall remove all tabs
   * It shall register an alarm for each tab
   * It shall reset the alarm when a tab is accessed
   * It should pause the alarms when in standby
   */
});
